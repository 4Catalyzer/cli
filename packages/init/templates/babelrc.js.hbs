module.exports = api => ({
  presets: [
    [
      '@4c/4catalyzer',
      {
        target: '{{type}}',
        modules: api.env() === 'esm' ? false : 'commonjs'
      },
    ],
    {{~#if typescript~}}
    '@babel/preset-typescript',
    {{~/if~}}
  ]
});
