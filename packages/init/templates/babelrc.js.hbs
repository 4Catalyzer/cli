module.exports = {{#if (eq type 'web')}}api => ({{/if}}{
  presets: [
    [
      '@4c',
      {
        target: '{{type}}',
        {{#if (eq type 'web')}}
        modules: api.env() === 'esm' ? false : 'commonjs'
        {{/if}}
      },
    ],
    {{~#if typescript~}}
    '@babel/preset-typescript',
    {{~/if~}}
  ]
}{{#if (eq type 'web')}}){{/if}};
