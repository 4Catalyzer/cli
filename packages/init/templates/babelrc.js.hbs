module.exports = api => ({
  presets: [
    [
      '@4c',
      {
        target: '{{type}}',
        {{#if (eq type 'web')}}
        modules: api.env() === 'esm' ? false : 'commonjs'
        {{/if}}
      },
    ],
    {{~#if typescript~}}
    '@babel/preset-typescript',
    {{~/if~}}
  ]
});
